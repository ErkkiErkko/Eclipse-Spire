# 莉雅的逐帧出牌动作

旧版攻击和防御只有整张立绘位移与旋转，技能和能力也只共用一张施法姿势。那不等于实现了人物肢体动作。本次改为实际角色绘制帧，并重做了首次预览中不自然的戳刺动作。

| 行为 | 人物动作 | 播放段 | 时长 |
| --- | --- | --- | --- |
| 攻击 | 抬刀蓄势、屈肘带刀掠过胸前、斜向劈砍、下沉收刀、恢复站姿 | 8 | 560 ms |
| 防御 | 屈膝、抬起持刀手架在胸前、伸掌抵挡、收回手臂 | 6 | 460 ms |
| 技能 | 空手收至胸前、抬臂摊掌引导月辉、落手归位 | 6 | 520 ms |
| 能力 | 收手至腰间、抬手聚力于胸前、短暂保持、落手归位 | 6 | 600 ms |

攻击、防御与技能有各自绘制的角色姿势。能力复用技能中的准备、收手、抬手三种姿势，按不同的顺序和保持时间组成六个播放段，并配合金色强化效果。复查时移除了会让双腿突然并拢、人物比例改变的独立闭眼姿势。

## 刀刃与握持

攻击采用八帧不带武器的身体动作，以及一张固定的单刃月刀图。月刀有明确的青色刀背、明亮的弧形刃口、金色圆护手与缠带刀柄。它在所有攻击帧中只旋转和等比缩放，绝不镜像，避免逐帧重绘武器造成刃口翻转。

八帧握刀手坐标记录在 `lib/hero-attack-rig.ts`，直接取自绘制帧。蓄势后，刀身角度按 -105°、-35°、20°、50° 连续沿同一方向完成劈砍。斩击段握刀手的水平变化只有约 31 像素，垂直变化约 320 像素（768 像素逻辑画布）；手保持在上身附近，通过屈肘、肩部和躯干转动带动刀刃划弧，而不是向敌人伸直手臂戳刺。

刀先画在身体前，再用原始角色图中的手指区域遮住刀柄，保持握持关系。画布在头顶留有额外空间容纳抬刀，人物脚底、血条、名称和点击区域保持原位。刀光是附加效果，实际角色动作可以脱离刀光单独播放与检查。

## 播放与节奏

- 只有成功结算的卡牌才触发动作；25 种可打出的卡牌均按主效果分类。
- 卡牌效果、资源扣除和手牌更新立即生效。播放器没有出牌锁和动作队列。
- 新动作截取已经显示的像素，用 40 ms 衔接进入新动作，避免快速连出时闪回原始姿势。
- 同时只有一个 requestAnimationFrame 循环。取消、卸载、场景切换和减少动态效果会终止旧回调。
- 图集和攻击用月刀都预加载，资源未就绪时保留完整待机角色，不排队补播过期动作。
- 受击仍采用 240 ms 的短促整体震动，不将其描述为肢体绘制动画。

## 资源

使用内置 imagegen，以原有 `public/assets/characters/hero-cute-v2.webp` 为角色身份与画风参考。原有立绘保留，未使用原游戏美术。

交付资源位于 `public/assets/characters/motion/`：

- `attack-v2.webp`：四列两行的八帧身体图，每格 768 × 768。
- `moon-katana.webp`：固定月刀贴图，保留原始透明通道。
- `guard.webp`、`skill.webp`、`power.webp`：三列两行，每格 768 × 768。
- `idle.webp`：资源加载期间的待机回退；攻击资源就绪后使用新攻击的准备姿势和固定月刀作为待机。

原始图的格子并不完全等距，直接均分会裁断头发、鞋底或武器。按原生透明人物轮廓分离完整帧，保留其 RGBA 像素，并对齐脚底与腰部锚点。同一动作各帧使用相同缩放比例，保留屈膝造成的高度变化。图集采用 WebP 质量 92、透明度质量 100。没有用脚本抠除背景或重新绘制角色。

本轮原始提示词、透明背景修正提示词、被弃用候选和最终选用规则分别记录在 `art-history/hero-sequence-prompts.json` 与 `art-history/hero-slash-prompts.json`。

## 实际检查

逐张查看四类动作的全部绘制帧，并用游戏实际 `createHeroRenderer` 与 `HeroMotionPlayer` 在原生 Canvas 中导出正常速度动画和逐帧对照图，检查：握刀手、刃口方向、挥刀轨迹、肢体连续性、脚底、边界裁切和回位。此检查不是浏览器截图，也没有声称已完成浏览器内出牌录像验收：当前浏览器工具连接失败。

32 项规则及播放器测试覆盖全部卡牌、动作分类、逐帧播放、连续出牌、取消和资源回退。额外实际渲染检查验证四套图集成功解码、攻击显示八个播放段、其他动作显示六个播放段、动作在 600 ms 内归位，且中断衔接起点的可见像素差为 0。类型检查与生产构建另行执行。
